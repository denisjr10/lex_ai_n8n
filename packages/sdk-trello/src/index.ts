/**
 * @lex/sdk-trello — a API do Trello, na parte que interessa
 * ---------------------------------------------------------------------------
 * Cobertura CURADA, e não total: o Trello tem superfície enorme e o projeto usa
 * um pedaço pequeno dela. Cobrir o que não se usa é manter o que ninguém testa.
 *
 * Não decide privilégio (Spec §3, regra 2).
 *
 * O recurso escasso aqui não é dinheiro, é VAZÃO (limite de requisições por
 * tempo). No chassi os dois são a mesma abstração (D-73), o que evita ter dois
 * mecanismos de reserva e disjuntor fazendo a mesma coisa com nomes diferentes.
 *
 * Preenchido depois do MARCO 7 (Spec §15).
 */
export {}
