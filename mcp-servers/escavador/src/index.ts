/**
 * @lex/mcp-escavador — o servidor MCP do Escavador
 * ---------------------------------------------------------------------------
 * 15 ferramentas, expostas por PERFIL: o mesmo servidor mostra conjuntos
 * diferentes conforme quem está do outro lado. O agente do cliente não vê as
 * ferramentas que gastam crédito; o agente do advogado vê.
 *
 * Regra 3: regra de negócio do escritório NÃO entra aqui. Este servidor é
 * genérico e reutilizável — a regra vive no n8n ou no Policy Gate. Toda vez que
 * uma "exceção do escritório" parecer caber neste diretório, é sinal de que ela
 * pertence a outro lugar.
 *
 * Preenchido no MARCO 7 (Spec §15).
 */
export {}
